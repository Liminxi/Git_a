<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yr.dao.oneToduo.ClassesMapper">


	<!-- 一对多关联查询 -->
	<!-- 方式一: 嵌套结果: 使用嵌套结果映射来处理重复的联合结果的子集 select * from class c,teacher t,student 
		s where c.teacherId=t.t_id and c.c_id = s.classId and c.c_id =1 -->
	<resultMap type="Classes" id="ClassResultMap3">
		<id property="id" column="c_id" />
		<result property="name" column="c_name" />

		<association property="teacher" column="teacherId"
			javaType="Teacher">
			<id property="id" column="t_id" />
			<result property="name" column="t_name" />
		</association>

		<!-- collection表示集合 property 对像中的属性名 ofType指定students集合中的对象类型 -->
		<collection property="stuList" ofType="student">
			<id property="id" column="s_id" />
			<result property="name" column="s_name" />
		</collection>
	</resultMap>

	<select id="getClass3" parameterType="int" resultMap="ClassResultMap3">
		select *
		from class c,teacher t,student s where c.teacherId=t.t_id and c.c_id =
		s.classId and c.c_id = #{id}
	</select>


	<!-- 方式二：嵌套查询：通过执行另外一个SQL映射语句来返回预期的复杂类型 SELECT * FROM class WHERE c_id=1; 
		SELECT * FROM teacher WHERE t_id=1 //1 是上一个查询得到的teacher_id的值 SELECT * FROM 
		student WHERE classId=1 //1是第一个查询得到的c_id字段的值 -->
	<resultMap type="Classes" id="ClassResultMap4">
		<id property="id" column="c_id" />
		<result property="name" column="c_name" />

		<association property="teacher" column="teacherId"
			javaType="Teacher" select="getTeacher2" />

		<!-- collection表示集合 property 对像中的属性名 ofType指定students集合中的对象类型 -->
		<collection property="stuList" ofType="Student" column="c_id"
			select="getStudent" />
	</resultMap>

	<select id="getClass4" parameterType="int" resultMap="ClassResultMap4">
		select *
		from class where c_id = #{id}
	</select>

	<select id="getTeacher2" parameterType="int" resultType="Teacher">
		select
		t_id id,t_name name from teacher where t_id = #{id}
	</select>

	<select id="getStudent" parameterType="int" resultType="Student">
		select
		s_id id,s_name name from student where classId = #{id}
	</select>
</mapper>